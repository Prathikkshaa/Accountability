// No auth yet: the app is scoped to a single "current user". Tara is the
// seeded account with partners, a group, goals and nudges, so every screen
// renders populated. Swap this (or read from localStorage) when auth lands.
export const CURRENT_USER_ID = 'user_tara';
